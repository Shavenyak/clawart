services:
  - type: web
    name: clawart
    runtime: docker
    plan: starter
    numInstances: 1
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    disk:
      name: clawart-room-data
      mountPath: /app/server/data
      sizeGB: 1
